package provider

// version is set at build time via ldflags:
//
//	go build -ldflags "-X github.com/anthropics-contrib/terraform-provider-anthropic-managed-agents/internal/provider.version=1.0.0"
var version = "dev"
